@model Chambers.ViewModels.HomeViewModel
@using Chambers.Custom.Extensions

@using (Html.BeginForm("Story", "Home", FormMethod.Post, new { @class = "col-md-6 form-block", data_toggle = "validator" }))
{
<div>
Left Player
@Html.DropDownList("LeftArbiter", Model.Characters.ToSelectList(c => c.Name, c => c.CharacterId.ToString(), "--"))<br/>

Affliction
@Html.DropDownList("LeftArbiterAffliction", Model.ArbiterAfflictions.ToSelectList(a => a.Name, a => a.AfflicationId.ToString(), "--"))<br/>
</div>

<div>
Right Player
@Html.DropDownList("RightArbiter", Model.Characters.ToSelectList(c => c.Name, c => c.CharacterId.ToString(), "--"))

Affliction
@Html.DropDownList("LeftArbiterAffliction", Model.ArbiterAfflictions.ToSelectList(a => a.Name, a => a.AfflicationId.ToString(), "--"))<br />
</div>


<div>
Left Victim
@Html.DropDownList("LeftVictim", Model.Characters.ToSelectList(c => c.Name, c => c.CharacterId.ToString(), "--"))

Affliction
@Html.DropDownList("LeftVictimAffliction", Model.VictimAfflictions.ToSelectList(a => a.Name, a => a.AfflicationId.ToString(), "--"))<br />
</div>

<div>
Right Victim
@Html.DropDownList("RightVictim", Model.Characters.ToSelectList(c => c.Name, c => c.CharacterId.ToString(), "--"))

Affliction
@Html.DropDownList("RightVictimAffliction", Model.VictimAfflictions.ToSelectList(a => a.Name, a => a.AfflicationId.ToString(), "--"))<br />
</div>
}
